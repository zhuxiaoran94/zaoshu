# 路线图

## 已实现

- [x] 120+ 内置字段生成器
- [x] 用户、电商、金融、游戏、社区、物流、测试通用模板
- [x] 多表主外键关联、唯一约束和固定随机种子
- [x] 随机、真实分布、边界、异常和 Pairwise 模式
- [x] 状态链、质量检查、覆盖报告
- [x] JSON、CSV、XLSX、三种 SQL、Postman、Fixture 导出
- [x] Web Worker 大数据生成及取消
- [x] 浏览器本地数据池、项目备份与恢复
- [x] 安全配置导入、公式注入保护和 Cloudflare 安全响应头
- [x] 生成前约束冲突诊断与问题定位
- [x] 条件规则可视化编辑器
- [x] Pairwise 排除规则、强制组合、覆盖审计和导出
- [x] 状态链参数配置、异常类型选择和事件导出
- [x] PWA 离线使用和版本更新提示
- [x] 命名项目快照和版本差异查看
- [x] 字段统计、锁定字段和单行重生成
- [x] 结果编辑后自动重跑质量检查
- [x] OpenAPI 3.x JSON Schema 导入
- [x] JSON 样例结构与多数组推断
- [x] YAML、XML 及六类测试框架 Fixture 导出
- [x] ZIP 多文件交付包、Manifest 与 SHA-256 校验
- [x] 首屏生成引擎拆包、按需加载与构建性能预算
- [x] MySQL、PostgreSQL、SQLite DDL Schema 导入
- [x] 六种真实数值/日期分布、枚举权重与业务模板预设
- [x] 精确覆盖缺口分析、字段定位与一键最少补数
- [x] TypeScript interface/type 静态推断与类型引用建模
- [x] 字段筛选多选、批量规则配置与安全批量删除
- [x] 最近 10 次生成历史、质量摘要与一键复现
- [x] OpenAPI 3 YAML 与普通 YAML 样例安全导入
- [x] OpenAPI 本地 `$ref`、`allOf`、循环与深度保护
- [x] 结果列显隐、稳定排序、组合筛选与当前视图导出
- [x] 七类数据质量检查、问题行索引与点击定位
- [x] 白名单公式解析器、日期函数、依赖排序与循环检测
- [x] 不含生成结果的 URL fragment 项目分享与本地副本导入
- [x] GitHub Actions 测试/构建/安全审计质量闸门、CODEOWNERS 与 Dependabot
- [x] 移除存在已知高危漏洞的 SheetJS，使用受控只写 XLSX 导出器
- [x] 零配置 CLI、项目配置输入、可复现参数、质量退出码与机器摘要
- [x] 全局命令中心、键盘快捷键、焦点管理与手机端顶部操作收敛
- [x] 10 套常用字段套餐、冲突预览、安全批量应用与一次撤销
- [x] JSON Schema、OpenAPI、TypeScript、三方 DDL 和关系说明契约包导出
- [x] 外键种子随机、轮询均匀、热点 80/20 与严格一对一分配策略
- [x] 一对一容量预检、外键分布统计与关系策略质量检查
- [x] 按父记录生成指定范围子数据、多级动态基数、数量预估与逐父质量检查
- [x] 父表明细聚合、外键回填、多级跨表计算和业务金额一致性审计
- [x] 表级业务断言、失败/警告分级、逐行定位与契约导出
- [x] 项目时间基准、全日期生成器复现、CLI 覆盖与导出追溯
- [x] MSW v2 Mock API 交付包、内存 CRUD、分页/搜索/排序/字段筛选与数据复位
- [x] OpenAPI 3.1 CRUD 路径、浏览器/Node 启动器和机器可读路由清单
- [x] Mock API 延迟区间、确定性失败率/状态码、响应包裹与复位序列
- [x] Mock API 网络行为同步进入 UI、CLI、OpenAPI、配置、路由清单和 Manifest
- [x] Mock API 外键写入校验、restrict/cascade 删除策略和父子嵌套路由
- [x] 同一父子表多外键的无冲突嵌套路径与生成代码运行级测试
- [x] Mock API 独立 TypeScript/Vitest 工程、精确依赖、跨来源路由与开箱即跑的 HTTP 回归测试
- [x] 生成工程严格类型检查与 npm 官方 0 漏洞实装验收
- [x] Mock API 原子批量写入、失败索引、整批回滚、健康检查与整库复位接口
- [x] 批量/控制接口同步进入 OpenAPI、路由清单、README 与独立工程测试
- [x] Mock API 单路由延迟、失败率、状态码与错误消息覆盖，最多 20 条安全规则
- [x] 路由规则 UI/CLI、Schema 存在性校验、命中响应头、OpenAPI 扩展与独立工程自测
- [x] Mock API 最近 500 条脱敏请求轨迹、稳定请求 ID、筛选查询与单独清空
- [x] 请求追踪 OpenAPI/路由清单/README 契约、浏览器与 Node 导出及隐私回归测试
- [x] Mock API 命名整库场景快照、同名覆盖、恢复、列表、单删与全清
- [x] 快照名称白名单、10 个数量上限、单个 5 MiB 限制、OpenAPI 契约与独立工程自测
- [x] Mock API 原子批量修改、批量删除、失败索引与重复 ID 防护
- [x] 批量删除 restrict/cascade 引用策略、级联数量、UI 路由场景与 OpenAPI 契约
- [x] Mock API 受限 JSON 跨表原子事务与 create/update/delete 动作
- [x] 事务安全别名引用、100 步/1 MiB/10 层限制、整库回滚与 OpenAPI 契约
- [x] Mock API 可选严格 Schema 请求校验与网页/CLI 开关
- [x] 未知字段、类型、必填、空值、枚举、数值范围、文本长度和不可变主键校验
- [x] 单条、批量、跨表事务统一 422 field/rule/index 错误定位与 OpenAPI 契约
- [x] Mock API 主键外 unique 字段的单条新增/修改冲突校验
- [x] 批量新增/修改与跨表事务唯一冲突定位及原子回滚
- [x] 唯一字段 409 field/rule=unique 错误契约与独立工程自测
- [x] Mock API 单条新增、批量新增与跨表事务 Idempotency-Key 幂等重试
- [x] 同键同请求并发合并与稳定响应重放、同键异参 409 冲突保护
- [x] 100 项/10 MiB 本地缓存上限、1 MiB 请求上限、清理接口与隐私追踪约束
- [x] 幂等请求头/响应头、409/413、健康状态与独立工程 HTTP 自测契约
- [x] Mock API 强 ETag、If-None-Match 条件读取与 304 响应
- [x] 单条 PATCH/DELETE If-Match 乐观锁、412 最新版本提示与过期写保护
- [x] 批量修改/删除逐项 ifMatch 与跨表事务动作级版本前置条件
- [x] 过期版本批量/事务原子回滚、请求头长度限制、隐私追踪与 OpenAPI 契约
- [x] Mock API 数值/日期比较、文本匹配、集合与空值高级字段筛选
- [x] 最多 5 字段稳定排序与降序前缀，兼容原 `_order` 单字段语义
- [x] 查询参数/名称/值/in 集合/排序字段资源上限与 400 错误保护
- [x] 高级查询 OpenAPI 扩展、包内说明与独立工程真实 HTTP 测试
- [x] Mock API 当前 Schema 业务路由调用次数与响应状态码预期声明
- [x] pending/passed/failed 逐条验收、状态码分布与 200/409 汇总接口
- [x] 规则计数复位、单删/全清、100 条/10,000 次/20 状态码安全上限
- [x] 调用预期 OpenAPI、路由清单、健康状态、复位隔离与独立 HTTP 自测
- [x] Mock API `_cursor=start` Keyset 游标分页与 X-Next-Cursor 响应头
- [x] 游标绑定资源/筛选/排序、进程随机签名与篡改/跨查询复用拒绝
- [x] 多字段排序追加主键稳定消歧、翻页期间增量写入与无重复续读
- [x] 页码/游标双模式 OpenAPI meta、4,200 字符上限与独立 HTTP 自测
- [x] Mock API 列表和单条 GET `_fields` 白名单响应裁剪
- [x] `_expand` 一层父表关联展开、缺失父记录安全返回 null
- [x] 50 字段/5 关系/1 层深度资源上限与非法关系拒绝
- [x] 响应形态绑定游标、完整记录 ETag、OpenAPI 扩展与独立 HTTP 自测
- [x] Mock API 单条 PUT 完整替换与 PATCH 部分修改的严格语义区分
- [x] PUT 必填字段、URL 主键、唯一字段、外键与 If-Match 统一保护
- [x] PUT 路由场景 UI/CLI、调用预期、请求追踪、OpenAPI 与独立 HTTP 自测
- [x] Mock API 页码/游标列表集合 ETag 与 If-None-Match 条件轮询
- [x] 集合 ETag 绑定完整底层页、响应裁剪、展开父记录、总数和游标元数据
- [x] 列表 304 保留分页响应头、512 字符上限、OpenAPI 与独立 HTTP 自测
- [x] 页码模式 RFC 8288 风格 first/prev/next/last 相对 Link 响应头
- [x] 游标模式签名 next Link、查询参数保留与请求 Origin 隔离
- [x] 200/304 Link OpenAPI 契约及独立 HTTP 续读测试

## 近期版本

- [x] 完整撤销/重做历史
- [x] 数据表新增、复制、删除和关系视图
- [x] 覆盖报告导出增强及未覆盖项补数
- [ ] 中英文界面切换
- [x] 第一阶段无障碍与移动端工作台优化（命令中心、焦点管理、390px 无溢出）
- [ ] 全量 WCAG 对比度、语义与屏幕阅读器审计
- [ ] 多列复合外键建模与导出
- [ ] 跨表业务断言、数值容差和条件生效范围
- [x] Mock API 针对单条路由覆盖延迟、失败率与自定义错误响应
- [x] Mock API 跨表事务脚本
- [x] Mock API 批量修改与批量删除
- [x] Mock API 自定义场景快照

## 后续探索

- [x] OpenAPI 3.x YAML 解析
- [ ] OpenAPI 多文件本地 `$ref` 解析
- [x] TypeScript 类型推断
- [ ] Web Streams 真正流式导出百万级数据
- [x] 可选的只读来源分享链接（接收者打开为可编辑的独立副本，不包含真实数据）
- [x] CI 接入
- [x] CLI 接入
- [ ] CLI 流式写出超大 JSONL / CSV
- [ ] 可选生成 Prism/WireMock/json-server 兼容工程

公开版本坚持纯前端优先：不开放数据库直连、不保存用户凭据、不提供匿名写入后端接口。
