# 更新日志

## 5.7.0 - 幂等重试版

### 可复现的真实重试流程

- 导出 Mock API 的单条新增、原子批量新增和跨表事务支持可选 `Idempotency-Key`，客户端超时重试不会制造重复记录。
- 同一键、方法、路径、查询与请求体返回首次响应快照，并用 `X-Mock-Idempotent-Replay: true` 标记；并发到达的相同请求会合并为一次实际执行。
- 首次响应用 `X-Mock-Idempotency-Stored` 说明是否缓存成功；同键异参或占用冲突返回 HTTP 409，键格式不合法返回 400。
- 网络故障注入位于幂等层之前，模拟连接失败不会进入缓存，测试代码可以按真实生产语义继续重试。

### 容量、隐私与契约

- 键只接受 1–80 位 ASCII 字母、数字、点、下划线、冒号或连字符；签名输入超过 1 MiB 返回 413。
- 响应缓存仅保存在当前浏览器或测试进程内存，最多 100 项且总计不超过 10 MiB；`DELETE /api/__mock/idempotency` 可单独清理。
- `resetMockData()` 同步清空幂等缓存，健康接口返回当前缓存项数；请求轨迹只记录是否重放，绝不记录幂等键或请求体。
- OpenAPI、路由清单和包内 README 声明请求头、响应头、400/409/413 与清理接口；独立工程新增真实 HTTP 重放测试。
- 总测试数增至 133；运行级测试覆盖并发合并、稳定响应、同键异参拒绝、隐私日志、缓存清理和复位隔离。

## 5.6.0 - 唯一约束版

### 更接近真实数据库的唯一性

- 导出 Mock API 现在识别每张表中除主键外的全部 `unique` 字段，覆盖手机号、邮箱、SKU、订单号、交易流水号等常见约束。
- 单条 POST 和 PATCH 在唯一值冲突时返回 HTTP 409，并提供 `field` 与稳定 `rule: unique`，调用方无需解析错误文案。
- 唯一性校验独立于严格 Schema 开关：兼容模式仍会阻止制造真实数据库无法接受的重复唯一值。
- 同一记录 PATCH 保持原值不会误报；只有命中其他记录或批次前序新增/修改后的值才算冲突。

### 原子性与契约

- 批量新增和批量修改实时检查当前批次前序结果；第 N 条冲突时返回 `index: N` 并恢复本批此前所有写入。
- 跨表事务中的 create/update 同样执行唯一性检查，后续动作冲突会回滚事务中的所有表。
- OpenAPI 错误规则新增 `unique`，POST/PATCH 的 409 描述同步区分主键与唯一字段。
- 总测试数增至 132；运行级测试覆盖单条新增/更新、同批新增、同批修改与事务第二步冲突的完整回滚。
- 独立工程测试增至 10 组，直接复用真实生成记录验证新增和修改的唯一冲突响应。

## 5.5.0 - 请求校验版

### 可选的真实后端校验体验

- Mock API 配置新增 `validateSchema`；开启后，POST/PATCH 会按当前字段设计校验未知字段、数据类型、必填、可空、枚举、数值最小/最大值和文本最大长度。
- PATCH 在严格模式下明确拒绝请求体中的主键字段，避免“返回成功但静默忽略”的误导；兼容模式仍保留原有的未知字段过滤行为。
- 校验错误统一返回 HTTP 422，并提供 `field` 和稳定 `rule`：`unknown_field`、`immutable`、`required`、`nullable`、`type`、`enum`、`minimum`、`maximum`、`max_length`。
- 单条 CRUD、批量新增/修改和跨表事务共享同一校验器；批量与事务失败额外返回 `index`，并保持原子回滚。

### 配置、契约与验证

- 导出面板新增“严格校验类型与约束”开关；默认关闭，避免破坏依赖宽松 Mock 的已有用法。
- CLI 新增 `--mock-api-strict-schema`；`config.ts`、Manifest、`routes.json` 与 `x-mock-api-behavior` 均保留最终开关值。
- OpenAPI 在严格模式下为新增请求声明必填字段、为 PATCH 排除主键，并为写接口声明带 `field/rule/index` 的 422 错误结构。
- 总测试数增至 131；运行级测试从合法生成记录出发逐项变异，覆盖未知字段、必填、类型、枚举、最小值、最大长度、不可变主键和批量失败回滚。
- 独立工程测试增至 9 组；无论导出配置是否开启严格模式，基础 CRUD 用例保持兼容，严格校验用例会显式开启后验证 422 定位信息。

## 5.4.0 - 跨表事务版

### 一次完成业务链路造数

- 导出的 Mock API 新增 `POST /api/__mock/transactions`，可按顺序原子执行最多 100 个跨表 `create`、`update`、`delete` 动作。
- 动作可用 `as` 保存结果别名，并在后续值或 ID 中通过 `$alias.field` 引用，例如创建用户后直接把 `$user.id` 写入新订单外键。
- 成功响应返回逐步结果、动作索引、资源、变更记录与别名清单；适合登录前置、订单支付、退款回滚等联调用例准备。
- 删除动作遵循当前 `restrict/cascade` 策略，更新和创建继续执行 Schema 字段白名单与外键完整性检查。

### 安全解释器与整库回滚

- 事务只解释固定 JSON，不执行 JavaScript、不接受脚本表达式、不请求任意 URL；资源必须是当前 Schema 的真实表。
- 每个动作只接受对应白名单字段，别名限制为 1–30 个 ASCII 字母、数字或下划线且必须以字母开头，禁止重复和前向引用。
- 请求体最多 1 MiB、最多 100 步、递归值最多 10 层；嵌套对象使用无原型容器构造，降低特殊键污染风险。
- 任一步业务失败或意外运行时异常都会恢复全部数据表到事务前状态，错误响应保留失败 `index`。

### 契约与验证

- OpenAPI 3.1 描述数组/对象两种事务请求、三类动作、资源枚举、别名规则、成功元信息与 400/404/409/413/422 状态。
- `routes.json` 与包内 README 同步新增事务控制接口和安全边界。
- 总测试数增至 130；运行级测试覆盖跨表外键别名、更新、删除、未知别名回滚、非法别名、101 步和超过 1 MiB 拒绝。
- 独立工程测试增至 8 组，真实请求验证创建父记录、引用生成主键创建子记录、更新与清理闭环，以及中途失败整库无变化。

## 5.3.0 - 批量变更版

### 原子批量增改删

- 每张业务表的统一 `/_batch` 路由从批量新增扩展到 POST、PATCH、DELETE 三种方法，每批限制 1–1,000 条。
- 批量修改接受 `[{ id, changes }]` 或 `{ items: [...] }`，禁止通过 changes 修改主键；成功返回修改后记录与 `meta.updated`。
- 批量删除接受 ID 数组或 `{ ids: [...] }`，成功返回直接删除的记录、`meta.deleted` 和额外级联的 `meta.cascaded`。
- 批量路由注册在 `/:id` 之前，避免真实 MSW 请求把 `_batch` 误识别为普通主键。

### 失败不污染测试数据

- 批量修改遇到非法格式、重复 ID、记录不存在或无效外键时恢复本表快照，之前已修改的行不会残留。
- 批量删除在写入前完整验证格式、重复 ID、记录存在性和引用策略；`restrict` 下任一目标被引用则整批不删除。
- `cascade` 下批量删除递归清理各目标的全部后代，并报告实际级联数量；异常时恢复整库快照。
- 错误响应延续从 0 开始的 `index`，可直接定位失败输入；批量接口仍遵循确定性延迟、故障注入和请求追踪。

### 契约、配置与验证

- OpenAPI 3.1 完整描述三种批量请求体、200/201/400/404/409/422 状态与引用策略；路由清单新增 `batchMethods`。
- 导出设置的单路由场景新增“批量修改/批量删除”选项，CLI 路由白名单也同步允许 PATCH/DELETE `/_batch`。
- 总测试数增至 129；运行级测试验证批量修改中途失败回滚、restrict 删除整批拒绝和 cascade 多父记录级联清理。
- 独立工程仍保持 7 组测试，其中批量测试扩展为真实 PATCH/DELETE 请求，防止 `_batch` 路由优先级回归。

## 5.2.0 - 场景快照版

### 可恢复的测试现场

- 导出的 Mock API 新增命名整库快照，可在退款、删除、异常状态等破坏性流程前保存现场，完成断言后恢复所有业务表。
- `POST /api/__mock/snapshots` 创建快照，同名返回 200 并安全覆盖；`GET /api/__mock/snapshots` 按修订号倒序列出名称、体积、总行数和分表行数。
- `POST /api/__mock/snapshots/:name/restore` 恢复整库，`DELETE /api/__mock/snapshots/:name` 删除单个，`DELETE /api/__mock/snapshots` 清空全部。
- 浏览器与 Node 入口同步导出保存、列表、恢复、删除和清空函数，测试代码既可发 HTTP 请求，也可直接控制内存现场。

### 安全与隔离

- 快照名称限制为 1–40 个中英文、数字、下划线或连字符，拒绝路径穿越式名称和任意载荷字段。
- 每个进程最多 10 个快照，单个序列化体积最多 5 MiB，避免公共演示页面被快照无限占用内存。
- 快照不写 IndexedDB、不上传服务器，只活在导出工程自己的浏览器或测试进程中；控制接口不经过延迟和故障注入。
- `resetMockData()` 同时清理快照、请求轨迹和确定性网络序列，确保测试用例之间不会残留状态。

### 契约与验证

- OpenAPI 3.1、`routes.json` 和包内 README 完整声明 5 条快照控制路由、请求体、成功/错误状态与资源上限。
- 总测试数增至 128；运行级测试覆盖多表修改后的整库恢复、同名覆盖、非法名称、第 11 个快照拒绝、单删和 reset 隔离。
- 独立工程测试增至 7 组，真实验证保存、业务写入、恢复和删除闭环。

## 5.1.0 - 请求追踪版

### 本地可观测性

- 每个业务请求生成稳定 `mock-{seed}-{sequence}` 请求编号，并通过 `X-Mock-Request-Id` 返回；复位后序列从 1 重新开始，回归结果可重复。
- 内存环形日志只保留最近 500 条，记录请求序号、方法、pathname、响应状态、实际延迟、是否注入故障及命中的单路由规则。
- `GET /api/__mock/requests` 倒序查询日志，支持 `_limit=1..500`、`method` 和 `status` 筛选；`DELETE /api/__mock/requests` 只清日志、不改变业务数据。
- `GET /api/__mock/health` 新增当前日志数量，`POST /api/__mock/reset` 同时清空数据、请求计数、日志和追踪序号。

### 隐私与契约

- 日志只保存 URL pathname，明确不读取或记录 query、请求体、Authorization、Cookie 及其他 header，避免测试凭据和业务载荷进入调试轨迹。
- 浏览器和 Node 入口均导出只读使用的 `requestLog` 引用，既可通过控制接口查看，也可在测试进程内直接断言。
- OpenAPI 为全部业务响应声明 `X-Mock-Request-Id`，并完整描述轨迹查询参数、日志结构、清空接口和不记录敏感数据的边界。
- `routes.json` 与包内 README 同步列出四个控制接口；控制接口仍不经过延迟或故障注入，也不会把自身查询写入业务轨迹。

### 验证

- 总测试数增至 127；专项测试连续执行 505 次请求验证只保留序号 6–505，并断言日志不包含 query token 或 Authorization 内容。
- 真实导出工程通过严格类型检查、6 组 HTTP/MSW 测试和 npm 官方 0 漏洞审计；生产首屏保持约 448.0 KiB。

## 5.0.0 - 路由场景版

### 精确到接口的故障模拟

- Mock API 网络场景新增最多 20 条单路由覆盖，可分别设置方法、路径、延迟区间、确定性失败率、HTTP 状态码和自定义错误消息。
- 命中规则后完全覆盖全局延迟与失败设置，未配置接口继续使用全局场景；相同 seed、请求和次序仍可重复复现。
- 路径参数使用 `:id` 匹配实际值，列表、单条、批量和父子嵌套路由都可定向配置。
- 命中规则的响应返回 `X-Mock-Route-Override: METHOD /api/path`，便于自动化断言实际采用了哪条场景。

### 安全可用的配置体验

- 导出面板根据当前 Schema 自动列出可用路由，用户无需手填路径；支持添加、切换、编辑和删除规则。
- 规则限制为 GET/POST/PATCH/DELETE、本地 `/api/` 路径、0–10 秒延迟、0–100% 失败率、400–599 状态码和 200 字符消息；控制接口不可覆盖。
- 重复方法与路径以最后一条为准；项目中不存在或关闭嵌套后失效的规则会被过滤，CLI 会在生成前明确拒绝未知路由。
- 390×844 手机视口实测页面、弹窗和规则行无横向溢出，全部输入和删除操作均有可访问名称。

### CLI、契约与验证

- CLI 新增可重复参数 `--mock-api-route 'METHOD|path|min:max|rate|status|message'`，适合在 CI 中声明支付限流、登录超时等场景。
- `config.ts`、`routes.json`、Manifest 和包内 README 保留归一化后的规则；OpenAPI 操作新增 `x-mock-route-override`、对应故障响应和覆盖语义。
- 独立工程自测会隔离规则完成普通 CRUD 回归，再单独强制首条规则到 100% 验证目标路径、状态码、消息和响应头。
- 总测试数增至 126；真实导出包已通过严格类型检查、5 组 HTTP/MSW 测试及 npm 官方 0 漏洞审计，生产首屏约 448.0 KiB。

## 4.9.0 - 批量联调版

### 原子批量造数

- 每张表新增 `POST /api/{table}/_batch`，接受 JSON 数组或 `{ items: [] }`，单次限制 1–1,000 条。
- 批量记录复用单条 POST 的字段白名单、数值/UUID 主键生成、重复主键检测和外键完整性检查，行为不会分叉。
- 任一项格式错误、主键冲突或外键无效时恢复该表到请求前状态，错误体返回从 0 开始的 `index`，测试可精确定位失败输入。
- 成功批量响应遵循原始 JSON、`{ data }` 或 `{ data, meta }` 配置，meta 记录实际创建数量。

### 可控测试基线

- 新增 `GET /api/__mock/health`，返回项目 seed、数据表数及每张表当前行数，便于测试前置检查。
- 新增 `POST /api/__mock/reset`，通过 HTTP 恢复全部初始数据并清空确定性延迟/失败请求序列。
- 两个控制接口不经过延迟与故障注入，即使业务接口设置为 100% 失败也能检查和恢复；它们仅运行在使用者本地 MSW 内存中，不对公网开放。
- 控制 handler 固定置于最高优先级，避免用户 Schema 中存在 `__mock` 表时被通用路由抢先匹配。

### 契约与验证

- OpenAPI 3.1 为每张表声明批量请求的两种结构、1–1,000 条限制、201/400/409/422/注入失败响应以及回滚语义，并声明健康检查和复位路径。
- `routes.json` 新增每表 batch 路径和独立 controlRoutes；Web 导出说明也直接展示“批量回滚”。
- 总测试数增至 120；在全新目录实装导出工程，严格类型检查、4 组真实 HTTP/MSW 测试和 npm 官方 0 漏洞审计全部通过。

## 4.8.0 - 可运行工程版

### 解压即可测试

- Mock API ZIP 新增独立 `package.json`、`tsconfig.json`、`vitest.config.ts`、`.gitignore` 和 `handlers.test.ts`，不再要求使用者先创建项目脚手架。
- 解压后执行 `npm install && npm run typecheck && npm test` 即可完成严格类型检查和真实接口回归。
- 内置测试通过 MSW Node server 发起真实 Fetch 请求，覆盖列表分页与总数、POST/PATCH/DELETE、主键保护、数据复位、外键 422、删除策略与父子嵌套路由。
- 生成配置改用显式可编辑运行时类型，测试可以安全临时关闭延迟和失败注入，使用完毕后恢复初始数据与请求序列。

### 可移植性与供应链安全

- 所有接口 handler 改为跨来源路径匹配，解决 Node 测试使用绝对 URL 时相对路由无法命中的问题，同时兼容浏览器和任意本地测试域名。
- 生成工程声明 Node 18+，锁定 MSW、TypeScript、Vitest 与 Node 类型的精确版本，减少安装时间点导致的依赖漂移。
- 在真实导出包验收中发现并修复旧 Vitest 版本的 critical 安全通告，最终候选工程经 npm 官方审计为 0 漏洞。
- MSW JSON 响应边界使用官方 `JsonBodyType` 收窄，生成代码在 TypeScript strict 模式下无错误。

### 验证

- 总测试数增至 119；另在全新临时目录实际生成并解压电商 Mock API，依赖安装、严格类型检查和 3 组 HTTP 集成测试全部通过。
- 生产构建继续执行 Cloudflare/PWA 产物检查和 450 KiB 首屏预算。

## 4.7.0 - 关系接口版

### 真正理解多表关系的 Mock API

- 根据项目 Schema 的外键自动建立关系清单；POST/PATCH 默认验证父记录是否存在，悬空外键返回 HTTP 422 和具体关联字段。
- 删除父记录默认使用 `restrict`，存在任何直接子记录时返回 HTTP 409 与引用数量，避免一次误操作静默破坏整套 Mock 数据。
- 可显式切换 `cascade`，按依赖递归删除全部后代；电商用户删除会同时清理订单、订单明细和支付记录。
- 每条外键自动生成父子嵌套查询，例如 `GET /api/users/:id/orders`；父记录不存在返回 404，响应带总条数和既有网络场景头。
- 同一父子表存在多条外键时自动生成 `/by-字段名` 路径，避免后声明路由覆盖先声明路由。

### 可配置、可审计

- 导出中心新增“写入时校验外键”“父记录删除”“生成父子嵌套路由”设置，安全默认值分别为开启、阻止删除和开启。
- CLI 新增 `--mock-api-delete restrict|cascade`、`--mock-api-no-fk-check` 和 `--mock-api-no-nested`。
- `config.ts`、`routes.json`、包内 README、Manifest 和 OpenAPI 3.1 同步保存关系策略。
- OpenAPI 新增嵌套路径、外键 422 和被引用 409 响应，并准确区分哪些表存在外键或被其他表引用。

### 验证

- 新增生成 handlers 运行级测试，实际执行 POST、嵌套 GET、restrict DELETE 和 cascade DELETE，不只检查输出文本。
- 新增多外键路径冲突测试；总测试数增至 117，生产构建及 Cloudflare/PWA 产物检查通过，首屏约 447.3 KiB。

## 4.6.0 - 网络场景版

### 可复现的弱网与失败

- Mock API 导出面板新增延迟最小/最大值、确定性失败率、失败 HTTP 状态码和成功响应格式配置，无需手改生成代码。
- 延迟限制在 0–10,000 ms，失败率限制在 0–100%，状态码限制在 400–599；恢复默认即可得到无延迟、无注入失败的普通接口。
- 网络行为由项目 seed、请求方法、完整 URL 与同类请求次数稳定决定，相同操作序列可重复复现，不制造不可追踪的测试偶发失败。
- `resetMockData()` 除恢复初始 CRUD 数据外，同时清空请求序列，Vitest/Node 示例默认在每条用例后复位。
- 所有响应返回 `X-Mock-Latency`，注入失败返回 `X-Mock-Injected-Failure: true`，便于测试断言区分业务错误和网络场景。

### 响应契约与交付

- 成功响应支持原始 JSON、`{ data }` 和 `{ data, meta }`；列表的 meta 包含 page、limit 与 total。
- 新增独立 `config.ts`，让使用者接入后继续调整 seed、延迟、失败率、状态码与响应结构。
- OpenAPI 3.1 同步生成响应包裹、分页元信息、400/404/409/注入失败响应与 Mock 响应头；字段精确筛选参数也进入契约。
- `routes.json` 与 Manifest 同步记录网络行为，压缩包离开工具后仍能审计生成条件。
- CLI 新增 `--mock-api-latency`、`--mock-api-failure-rate`、`--mock-api-failure-status` 和 `--mock-api-envelope`，并阻止在非 Mock API 格式下误用。

### 验证与性能

- 新增参数规范化、CLI 边界、生成配置、响应契约、Manifest 与 handlers 网络逻辑测试，总测试数增至 114。
- 实际 CLI 验收 245 条电商数据、8 个 Mock API 文件与 Manifest；生产构建通过，网络配置 UI 按需加载，首屏 JavaScript 约 447.2 KiB。

## 4.5.0 - Mock API 交付版

### 从数据文件到可调用接口

- 导出中心新增“Mock API 项目”，一次生成 `db.json`、MSW v2 handlers、浏览器启动器、Node 测试服务器、OpenAPI 3.1、路由清单和接入 README。
- 每张表自动生成列表、单条、新增、局部修改和删除路由；列表提供全文搜索、任意 Schema 字段精确筛选、分页、升降序排序与 `X-Total-Count`。
- POST 可自动生成数值或 UUID 主键，重复主键返回 409；PATCH 保持原主键，找不到记录返回 404，非法 JSON 返回 400。
- `resetMockData()` 可让测试之间恢复固定初始数据，浏览器与 Vitest/Node 两种入口可直接接入 MSW v2。

### 契约与安全边界

- OpenAPI 3.1 新增全部 CRUD paths，不再只输出 Schema；可导入 Apifox、Postman 或 Swagger UI 查看和消费。
- 写请求按当前数据表字段白名单过滤，不接受 Schema 外属性；排序字段同样限制在字段清单内。
- `db.json` 自动剥离工具内部 `_mock_meta`，避免异常审计信息污染被测接口的数据结构。
- Mock API 产物只在下载者自己的浏览器或测试进程内运行，不在 Cloudflare 公共站点开放数据库或匿名写接口。
- Web UI 与 CLI 均支持 `mock-api` 格式，继续使用 ZIP Manifest、SHA-256、固定种子、时间基准及安全拒绝覆盖机制。

### 验证

- 新增路由一致性、生成 TypeScript 语法、字段白名单、复位、元信息清理、OpenAPI CRUD 和完整 ZIP 专项测试，总测试数增至 112。
- 生产构建及 Cloudflare/PWA 产物检查通过，Mock API 代码按需加载，首屏 JavaScript 约 446.5 KiB。

## 4.4.0 - 时间基准版

### 跨环境精确复现

- 项目新增可编辑时间基准，默认固定为 ISO 时间；相同 Schema、随机种子与时间基准跨日期、设备和 CI 生成相同业务数据。
- 顶部流水线直接显示日期选择器，并提供“设为现在”；选择当前时间适合临时联调，固定日期适合回归和缺陷复现。
- 旧项目与旧分享链接没有该属性时继续使用固定兼容基准，不会退回隐式系统时间。

### 统一时间语义

- 过去/未来日期、普通日期、日期时间、工作日、周末、月初、月末、最近时间戳和生日统一读取项目基准。
- ULID、雪花 ID、订单号、交易流水号和 `{date}` 格式模板不再写死历史日期，统一反映当前项目基准。
- 季度、财年和状态链事件的年份与起始时间同步跟随基准，避免同一项目内部出现互相矛盾的年代。
- 日期边界处理统一使用 UTC，避免部署地区和访问者本地时区造成月初、月末漂移。

### CLI、契约与追溯

- CLI 新增 `--reference-date`，支持 `YYYY-MM-DD` 或 ISO 时间，dry-run 和机器摘要返回最终基准。
- 项目文件、隐私分享、快照与差异、运行历史、JSON Schema、OpenAPI、关系说明和导出 Manifest 均保留时间基准。
- 导出中心、覆盖报告页面和 Markdown 报告显示基准日期，产物离开工具后仍能解释相对时间来源。

### 验证

- 新增相对时间复现、基准变化、状态链基准和 CLI 日期参数测试，总测试数增至 108。
- 生产构建及 Cloudflare/PWA 产物检查通过，首屏 JavaScript 约 445.6 KiB。

## 4.3.0 - 业务断言版

### 可配置业务规则

- 每张表可添加最多 20 条业务断言，用白名单公式表达跨字段一致性，表达式结果为 `true` 才通过。
- 断言编辑器直接位于当前表工具栏，包含规则名称、表达式、失败提示和失败/警告级别。
- 支持四则运算、比较、逻辑、三元条件、空值判断以及既有白名单函数，不使用 `eval`、`Function` 或输入代码执行。
- 生成前检查语法错误、缺失字段和重复规则 ID，存在阻断问题时不启动大批量生成。

### 质量与契约

- 生成后逐行执行断言，失败结果记录真实问题数量与最多前 200 个行号，点击即可定位数据。
- 普通模式按配置区分失败和警告；边界与异常模式的规则违例标记为预期异常。
- 内置电商模板加入“成功支付必须有支付时间”，与条件字段共同形成开箱即用的业务规则示例。
- JSON Schema、OpenAPI、项目配置、分享、快照和关系说明完整保留断言；关系 Markdown 新增业务断言清单。

### 验证

- 新增 4 组断言专项测试，总测试数增至 105；验证正常通过、数据篡改定位、警告级别、异常语义和非法配置拦截。
- 真实浏览器验证断言编辑器和内置支付规则，生产构建及 Cloudflare/PWA 检查通过，首屏 JavaScript 约 444.3 KiB。

## 4.2.0 - 业务金额闭环版

### 可用的跨表计算

- 字段设计器新增“汇总子表”和“从父表回填”两种跨表规则，无需编写脚本即可建立业务一致性。
- 明细汇总支持 SUM、COUNT、MIN 和 MAX；SUM/MIN/MAX 使用现有白名单公式表达式，并可设置 0–12 位小数精度。
- 父表回填根据当前表外键建立索引，可复制父表任意字段；同一表的多项规则都会执行。
- 聚合和回填混合链统一按字段依赖排序；循环依赖和失效字段会在生成前阻断。
- 聚合与回填均使用线性索引，安全表达式只解析一次，可处理大批量明细而不做逐行全表扫描。

### 电商金额闭环

- 内置电商模板自动计算 `orders.amount = SUM(order_items.price × quantity)`。
- 支付记录自动回填关联订单金额，默认生成结果形成“明细 → 订单 → 支付”金额闭环。
- 质量检查逐行复算跨表结果，手工修改订单或支付金额后可直接定位问题数据。
- 异常模式保留定向注入的金额不一致；明细中的类型异常继续作为测试样本保留，聚合器跳过不可计算值而不终止整批生成。
- 项目配置、隐私分享、JSON Schema、OpenAPI 和关系 Markdown 均完整保留跨表规则。

### 验证

- 新增跨表金额、可复现、篡改定位、无效表达式、同表多规则、混合依赖和异常保留专项测试，总测试数增至 101。
- 字段设计器继续按需加载；生产构建执行首屏 450 KB 预算与 Cloudflare/PWA 产物检查。

## 4.1.0 - 一对多基数版

### 真实业务数据量

- 数据表新增“按父记录生成子数据”，选择当前表任一外键，并配置每个父记录最少/最多子记录数。
- 子表总量由父表实际生成结果驱动，支持连续的多级关系；同一 Schema、种子和父表规模下逐父数量稳定可复现。
- 电商模板默认配置每个用户 1–3 个订单、每个订单 2–4 条明细，打开即可生成真实一对多数据。
- 侧栏、表工具条和关系页实时展示动态预计条数与基数规则；固定数量模式保持向后兼容。

### 完整性与安全

- 生成前检查动态单表 100,000 条、项目 200,000 条上限，并提示最大基数导致的资源风险。
- 阻止驱动外键同时使用唯一/一对一、空值、缺失或条件规则；删除驱动字段时自动关闭动态基数。
- 质量报告逐个父记录检查实际子记录数，手工删改破坏基数时可发现并定位。
- 单行重生成自动锁定基数驱动外键；动态基数表拒绝单独追加覆盖行，避免静默破坏父子数量。
- CLI dry-run 返回动态计划总量；`--count` 明确切换全部表回固定数量，便于批处理覆盖。
- 项目配置、分享、快照差异、JSON Schema/OpenAPI 扩展和关系 Markdown 均保留基数规则。

### 验证与性能

- 新增 4 组动态基数专项测试，总测试数增至 94；验证两级关系、可复现性、冲突诊断和手工破坏检测。
- 已在真实浏览器验证电商模板预计/实际 239 条、20 个用户各 1–3 单、40 个订单各 2–4 条明细。
- 新工具条拆为约 2.96 KB 按需模块；首屏 JavaScript 约 449.8 KiB，继续低于 450 KiB 发布预算。

## 4.0.0 - 关系分配版

### 可控外键造数

- 字段设计器为每个外键新增种子随机、轮询均匀、热点 80/20 和严格一对一四种分配策略。
- 轮询均匀按父表顺序分配，引用次数最多相差 1，适合分片、租户和负载均衡测试。
- 热点策略将约 80% 引用放入前部热点池，热点池比例可在 1%–50% 调整，适合缓存、热点账户和倾斜数据测试。
- 严格一对一按行唯一绑定父记录，选择策略时将空值率、缺失率归零，不会遗留额外的唯一选项状态。
- 四种策略均使用固定种子和稳定行序；整批生成、覆盖补数和单行重生成共享同一选择实现。

### 诊断、质量与可观测性

- 生成前检查严格一对一及其他唯一外键的父表容量；容量不足直接阻断并给出可操作建议。
- 严格一对一不允许随机空值或字段缺失，避免“配置看似一对一、结果并非一对一”。
- 质量报告新增一对一重复引用检查和轮询均衡度检查，可定位重复问题行。
- 关系页按真实依赖拓扑展示生成顺序；每条关系显示策略、父记录覆盖数、未使用数、最小/最大引用次数和 Top 1 占比。
- 项目配置 Zod 校验、隐私分享和备份恢复完整保留分配策略；热点比例限制为 1%–50%。
- JSON Schema/OpenAPI 的 `x-foreign-key` 与关系 Markdown 写入策略信息，方便接口评审和自动化消费。
- Web Worker 被受限 WebView、浏览器策略或 CSP 阻止启动时自动降级到主线程生成，不再让用户停在笼统失败状态。

### 验证

- 新增 5 组外键分配与容量专项测试，总测试数增至 90；全套测试、TypeScript 构建及 Cloudflare/PWA 产物检查通过。
- 首屏 JavaScript 约 445.8 KiB，继续低于 450 KiB 发布预算。

## 3.9.0 - Schema 契约版

### 新增

- 导出中心新增 Schema 契约包，不要求先生成测试数据；打开未生成项目时会默认选中该格式。
- 契约 ZIP 包含 JSON Schema 2020-12、OpenAPI 3.1、TypeScript 类型、MySQL/PostgreSQL/SQLite DDL 和 Markdown 关系说明。
- JSON Schema 与 OpenAPI 保留类型、必填、可空、范围、枚举、长度和常用 format。
- 使用 `x-mock-generator`、`x-mock-formula`、`x-mock-condition` 与 `x-foreign-key` 保留造数工具特有规则。
- 三种 DDL 按外键依赖顺序建表，包含主键、唯一、必填和外键约束，并按方言映射类型与安全引用标识符。
- TypeScript 接口包含字段中文说明、可选/可空语义、枚举字面量联合类型和外键注释。
- 关系说明列出生成顺序、每表默认条数、字段数及全部跨表引用。

### CLI 与交付完整性

- CLI 新增 `--format schema`，可从七套内置模板或网页导出的 `.mock.json` 直接生成契约包。
- Manifest 标记 `schema-contract` 类型、工具版本、Schema 版本、种子、生成时间，以及七个文件的字节数和 SHA-256。
- 契约导出器按需加载为约 6.1 KB 独立模块，首屏 JavaScript 约 440.3 KiB，继续低于 450 KiB 预算。
- 新增 4 组契约测试和 1 组 CLI 参数测试，总测试数增至 85。
- 已真实生成并解包电商契约 ZIP，验证七个契约文件与 Manifest；网页端验证无数据时可直接导出且无应用错误。

## 3.8.0 - 字段套餐版

### 新增

- 字段区新增“添加字段套餐”，可一次补齐常见业务和工程字段，无需逐个选择生成器。
- 内置用户身份、中国地址、组织员工、金额交易、审计字段、接口追踪、分页响应、文件信息、设备环境和软删除 10 套套餐。
- 套餐面板在应用前展示字段名、显示名、类型、数量和当前表应用后的总字段数。
- 自动检测与当前表同名的字段，默认跳过已有字段，也可选择自动生成 `_2`、`_3` 等安全名称。
- 字段套餐同时加入命令中心，手机端和键盘用户无需定位侧栏图标。

### 安全与一致性

- 应用套餐强制每表 200 字段上限，并同时检查整表字段名和字段 ID 唯一性。
- 每个新增字段使用全新 ID，套餐应用只产生一个撤销记录，可通过标准撤销完整恢复。
- 审计套餐的更新时间使用安全公式依赖创建时间；员工号使用格式模板，保证整组规则可直接生成。
- 修复数字/布尔 `customEnum`：生成值按字段类型转换，质量检查和覆盖报告使用相同归一化规则。
- 覆盖缺口中的数字枚举保持数字类型，一键最少补数不会重新写成字符串。

### 验证与性能

- 新增 5 组套餐专项测试，并对全部 10 套套餐逐一执行真实生成与质量检查，总测试数增至 80。
- 真实浏览器验证身份套餐在用户表中识别 4 个冲突、只新增 2 个字段，并可一次撤销。
- 套餐面板按需加载为约 7.6 KB 独立模块，首屏 JavaScript 约 439.8 KiB，继续低于 450 KiB 预算。

## 3.7.0 - 命令中心版

### 新增

- 顶部新增命令中心入口，使用 `⌘/Ctrl + K` 可在任何非阻塞状态快速打开或关闭。
- 动态提供 27 个左右的命令，覆盖生成、导出、项目、Schema、数据池、撤销/重做、五种模式、七套模板和八个结果面板。
- 搜索支持中文、英文别名和多关键词交集，例如“电商”“commerce”“质量 report”。
- 支持 ↑↓ 循环选择、Enter 执行、Esc 关闭，自动跳过不可用命令并显示实时状态提示。
- 新增 `⌘/Ctrl + Enter` 生成、`⌘/Ctrl + Shift + E` 导出、`⌘/Ctrl + Shift + P` 项目和标准撤销/重做快捷键。

### 无障碍与移动端

- 命令中心使用 dialog、combobox、listbox 和 option 语义，打开后聚焦搜索框，Tab 焦点保持在弹窗内，关闭后回到原控件。
- 其他弹窗打开时暂停后台快捷键，避免误触生成、导出或修改项目。
- 数据表图标操作补充明确的可访问名称。
- 手机端隐藏次要顶部动作并通过命令中心完整保留能力，390px 实测视口不再产生横向溢出。

### 性能与验证

- 命令中心视图按需加载为约 3.8 KB 独立模块，首屏 JavaScript 为约 437.6 KiB，继续低于 450 KiB 预算。
- 新增 3 组命令搜索测试，总测试数增至 74。
- 已在真实浏览器验证入口点击、懒加载、搜索执行、模式切换、`Ctrl+K`、自动聚焦、无控制台错误和移动布局。

## 3.6.0 - CLI 自动化版

### 新增

- 新增 `npm run mock -- ...` 命令，终端与 CI 可以直接复用网页端同一套生成、诊断、质量和导出内核。
- 默认使用电商内置模板，无需准备数据；支持 `users`、`commerce`、`finance`、`game`、`community`、`logistics` 和 `testing`。
- 支持读取网页导出的 `.mock.json`，并覆盖随机种子、造数模式、每表数量和 18 种导出格式。
- 支持 `--dry-run` 只校验计划、`--summary` 写机器 JSON 摘要、`--json` 输出机器结果、`--fail-on-quality` 以退出码 2 阻断 CI。
- 支持 `--list-templates` 和完整 `--help`，无需查看源码即可发现模板、模式和格式。

### 安全与自动化

- 配置文件限制为普通文件和 1 MB，继续通过严格项目校验与生成前诊断，不执行输入内容。
- 输出和摘要默认使用原子新建语义，已有文件会被拒绝；仅明确传入 `--force` 时允许覆盖。
- 禁止输出 ZIP 与摘要使用同一路径，dry-run 禁止输出参数，避免歧义和误写文件。
- CLI 不发起网络请求，不读取浏览器数据池或历史；默认安全输出名会移除路径与特殊字符。
- GitHub Actions 增加固定种子 dry-run 冒烟检查；新增 3 组参数解析测试，总测试数增至 71。
- 已真实验证多表 XLSX ZIP、Manifest、机器摘要以及重复输出拒绝行为。

## 3.5.0 - 公共安全版

### 供应链修复

- 移除存在原型污染与 ReDoS 已知高危问题、且 npm 版本无修复方案的 `xlsx` / SheetJS 依赖。
- 使用项目内受控的 Office Open XML 写入器保留多表 XLSX 导出，不再引入通用工作簿解析攻击面。
- XLSX 字符串继续中和公式前缀，并转义 XML、过滤非法控制字符、限制单元格长度与工作表名。
- 移除 9 个间接包和约 429 KB 的 XLSX 浏览器产物；官方 npm 生产依赖审计结果为零漏洞。

### 自动质量闸门

- 新增 GitHub Actions，对 `main` push 和 Pull Request 执行 `npm ci`、68 项测试、TypeScript 生产构建及产物检查。
- CI 验证 Service Worker、Cloudflare SPA 回退、安全响应头、CSP 禁止 `unsafe-eval` 和 450 KB 首屏 JavaScript 预算。
- 生产依赖出现 high/critical 漏洞时阻断，Pull Request 中新增的高危依赖也会单独审查。
- Workflow 使用只读仓库权限、不保留 checkout 凭据，并限制并发和最长运行时间。
- 新增 Dependabot 周期更新、CODEOWNERS、Pull Request 清单和脱敏 Bug 模板。

### 公开协作

- 新增 `CONTRIBUTING.md`，明确纯前端、本地处理、不可执行导入代码等贡献边界。
- 新增 `SECURITY.md`，引导通过 GitHub Security 私下报告漏洞，避免公开利用细节和真实数据。
- 新增 MIT License，明确允许公开使用、修改和分发，并要求保留版权与许可声明。
- 文档明确 Branch protection 仍需仓库管理员在 GitHub 设置中开启，仓库内配置无法自行强制保护主分支。
- 新增 XLSX 文件结构、多表输出和公式中和专项测试。

## 3.4.0 - 隐私分享版

### 新增

- 项目与本地数据面板新增“复制隐私分享链接”，可分享当前 Schema、字段规则、关系、模式和随机种子。
- 接收者打开链接后创建带“分享副本”标记的独立本地项目，不会覆盖或修改分享者的浏览器项目。
- 即使接收者已有本地项目，分享内容仍在本次打开时优先加载；原有本地数据池继续保留。

### 隐私与安全

- 分享内容只放在 URL `#fragment` 中，浏览器不会把 fragment 随 HTTP 请求发送给 Cloudflare。
- 链接不包含生成结果、自定义数据池、项目快照或运行历史，载入后会立即从地址栏移除。
- 分享内容继续复用项目文件的 Zod 严格校验、1 MB 配置上限与 200,000 行生成上限。
- 链接编码最大 60,000 字符；超限时明确要求改用项目配置文件，损坏、伪造及非法 UTF-8 内容均被拒绝。
- 分享链接是便捷传递规则的编码，不是加密或权限控制；任何持有者都能查看 Schema 并编辑自己的副本。
- 新增 3 组专项测试，覆盖中文无损往返、敏感本地数据排除和恶意载荷拒绝。

## 3.3.0 - 安全公式版

### 新增

- 计算字段支持括号、加减乘除、取模、比较、逻辑和三元条件表达式。
- 支持 `round`、`min`、`max`、`length`、`concat`、`coalesce`、`if`、`upper` 和 `lower`。
- 支持 `dateAdd` 与 `dateDiff`，时间单位覆盖毫秒、秒、分钟、小时、天和周。
- 计算字段依赖自动拓扑排序，可让后置字段引用其他计算字段。
- 编辑器帮助文案列出主要白名单能力。

### 安全与诊断

- 使用 tokenizer、递归下降 parser 和受控 AST 求值，不使用 `eval` 或 `Function`。
- 不支持属性访问、数组下标、赋值、分号、未知函数和任意 JavaScript。
- 生成前阻断语法错误、无效字符、字段缺失、自引用和计算字段循环依赖。
- `&&`、`||`、三元表达式和 `if` 使用短路求值，未命中分支不会被执行。
- 新增 6 组公式专项测试，覆盖正常运算、日期、字段引用、依赖排序和危险语法拒绝。

## 3.2.0 - 质量定位版

### 新增

- 质量引擎新增必填字段、数据类型、文本长度和枚举合法性检查。
- 原有唯一性、外键和数值范围检查新增字段 ID、问题数量和问题行号。
- 质量列表中的失败或预期异常项可以点击定位。
- 定位后自动切换到对应数据表、字段和表格视图，只显示问题记录。
- 问题行视图可一键恢复为全部数据。

### 准确性

- 条件字段不会被普通必填检查误报。
- 边界和异常模式中的定向问题标为预期异常，随机/真实分布模式仍标为失败。
- 行号最多存储前 200 个，但问题总数始终完整保留，避免报告对象无上限增长。
- 新增七类质量问题及真实行号定位测试。

## 3.1.0 - 结果探索版

### 新增

- 数据预览新增字段显示/隐藏菜单，以及全部显示和全部隐藏操作。
- 表头支持三态排序：升序、降序、恢复原始生成顺序。
- 数字、日期、布尔、文本和空值采用对应的比较规则。
- 搜索、异常过滤、排序和可见列可以组合使用。
- 支持将全部匹配记录和当前可见列直接导出 CSV。

### 一致性

- 视图排序和过滤只作用于派生结果，不改变原始生成数据。
- 表格中的编辑、删除和单行重生成继续使用原始行号定位。
- 页面仍只渲染前 100 条，但当前视图导出包含全部匹配结果。
- 新增筛选、异常行、数字/日期稳定排序和原数组不变测试。

## 3.0.0 - OpenAPI 复用模型版

### 新增

- 支持解析 `#/components/schemas/*` 形式的 OpenAPI 文档内部 `$ref`。
- 支持通过 `allOf` 合并继承字段、必填项、枚举和格式。
- 对象引用在目标模型拥有主键时自动转换为多表外键。
- 数组引用保留为嵌套数组字段，并在导入预览中提示当前关系处理方式。
- 对象和数组固定值按真实 JSON 值生成。

### 安全与诊断

- 明确拒绝 HTTP、HTTPS、文件路径等所有外部 `$ref`，不会产生网络请求。
- 缺失的本地引用直接阻止导入。
- 循环引用会被检测并安全降级，超过 20 层的展开会停止并告警。
- 新增 `$ref`、`allOf`、外部引用、缺失引用和循环引用专项测试。

## 2.9.0 - OpenAPI YAML 版

### 新增

- OpenAPI 3 导入新增 YAML 支持，可粘贴文本或读取 `.yaml` / `.yml` 文件。
- 普通 YAML 对象和对象数组也可沿用 JSON 样例推断能力。
- YAML 解析模块通过动态导入独立分包，仅在用户点击分析时下载。
- 性能预算从构建后的 HTML 精确识别首屏入口，避免同名异步分块造成误判。

### 安全

- YAML 采用严格文档模式，重复键、解析错误和所有未知标签告警均直接阻止导入。
- 别名展开数量限制为 100，继续执行 1 MB、50 张表和每表 200 字段限制。
- 不解析网络地址、不读取远程 `$ref`、不执行自定义标签或任何输入代码。
- 新增 OpenAPI YAML 推断、重复键和未知执行标签测试。

## 2.8.0 - 可复现运行版

### 新增

- 每次成功生成后自动记录一条运行历史，最多保留最近 10 次。
- 记录项目 Schema、随机种子、造数模式、数据条数、耗时、质量结果和覆盖缺口数。
- 项目面板新增“最近生成”，支持一键恢复当时配置并重新生成。
- 支持逐条删除运行历史，清除本地数据时也会同步清空。

### 隐私与容量

- 生成历史保存在当前浏览器 IndexedDB，不上传服务器。
- 不保存生成结果、自定义数据池值或导出文件，只保留复现所需配置与摘要。
- 读取历史时重新执行完整项目配置校验，损坏或过期记录自动忽略。
- 新增运行摘要内容和深拷贝隔离测试。

## 2.7.0 - 批量字段治理版

### 新增

- 字段列表新增批量配置模式和已选数量提示。
- 支持选择当前搜索结果、取消当前搜索结果和逐字段多选。
- 批量设置空值率、字段缺失率、异常率及六种真实分布。
- 支持批量删除选中字段。

### 安全与效率

- 被其他表外键引用的字段不能被批量删除。
- 每张表至少保留一个字段，阻止产生不可生成的空表。
- 一次批量修改或删除只写入一次撤销历史。
- 新增批量更新、单步撤销、引用保护和空表保护测试。

## 2.6.0 - TypeScript 建模版

### 新增

- Schema 入口新增 TypeScript 模式与可直接运行的双类型示例。
- 支持对象形式的 `interface` 和 `type`，以及 `export`、`declare`、`readonly` 和可选属性。
- 推断 `number`、`bigint`、`boolean`、`Date`、数组、对象和常见字符串生成器。
- 将字符串/数字字面量联合类型转换为可编辑枚举。
- 根据 `null`、`undefined` 和 `?` 推断空值率或字段缺失率。
- 类型名引用与常见 ID 字段自动连接至目标类型主键。

### 安全

- 仅解析类型声明文本，不调用 TypeScript 编译器，不导入模块，不执行输入表达式。
- 索引签名、方法、无法识别的语法和未导入类型会忽略或降级，并在确认前展示告警。
- 继续限制 1 MB 输入、50 张表和每表 200 个字段。

## 2.5.0 - 覆盖补数版

### 新增

- 覆盖引擎精确统计每个枚举候选值是否出现。
- 检查已配置的 NULL、字段缺失、最小值和最大值是否实际命中。
- 覆盖报告展示可定位到字段的缺口清单。
- 支持一键追加最少数量的记录，补齐当前所有可验证缺口。
- Markdown 报告新增未覆盖项章节。

### 一致性

- 补数记录自动生成唯一主键，并从已生成父表中选择有效外键。
- 同一字段的多个缺口错开写入，避免边界、NULL 或枚举值相互覆盖。
- 补数后同步表记录期望数量并重跑质量检查，保证数据量与唯一性检查不被破坏。
- 新增缺口识别、最少补数、主键唯一和质量检查专项测试。

## 2.4.0 - 真实分布版

### 新增

- 真实分布模式实现均匀、正态、长尾、热点、递增和递减六种算法。
- 数字、金额、比例、等级、库存、文件大小以及日期时间字段可应用分布规则。
- 枚举、自定义枚举和本地数据池支持权重抽样，权重无需手动合计 100。
- 字段设计器新增分布中心、权重配置和实时形态预览。
- 用户与电商模板预设会员等级、用户状态、订单状态、支付方式、金额和时间等业务分布。
- 覆盖报告新增真实分布配置统计。

### 质量

- 生成前检查权重数量不匹配、权重全零和分布中心越界。
- 相同项目、随机种子和规则仍产生可复现的分布结果。
- 新增权重抽样、趋势、正态均值、长尾特性和错误配置单元测试。

## 2.3.0 - SQL DDL 建模版

### 新增

- Schema 入口新增 SQL 模式与可直接运行的双表 DDL 示例。
- 支持 MySQL、PostgreSQL 和 SQLite 常见 `CREATE TABLE` 语法。
- 推断整数、小数、布尔、日期时间、JSON、UUID、IP 和字符串列。
- 识别行内及表级主键、唯一约束、外键，以及带名称的 `CONSTRAINT`。
- 支持 MySQL `ENUM` 候选值、字符串长度和小数精度。
- 支持反引号、双引号和方括号标识符。

### 安全

- DDL 仅作为文本解析，不连接数据库，也不执行任何 SQL。
- `DROP`、`INSERT` 等非建表语句会被忽略。
- 继续限制 1 MB 输入、50 张表和每表 200 个可用字段。

## 2.2.0 - 首屏性能版

### 性能

- 将 120+ 字段元数据与 Faker 运行时拆分，字段设计器不再加载完整 Faker。
- 首次生成统一交给 Web Worker；不支持 Worker 的环境才按需加载生成引擎。
- 单行重生成按需加载生成引擎，XLSX 仍只在用户选择 Excel 导出时加载。
- Pairwise、状态链、依赖排序和质量重算迁入轻量模块。
- 主页面 JavaScript 从约 948 KB 降至约 395 KB，体积减少约 58%。

### 质量保障

- Cloudflare 产物检查新增 450 KB 首屏 JavaScript 预算。
- 构建会验证 Faker 引擎已形成独立异步分块，防止后续改动意外回归首屏。

## 2.1.0 - 可审计导出版

### 新增

- 新增 YAML 与 XML 导出，并对 XML 属性和值进行完整实体转义。
- 新增 Playwright、Cypress、Jest、pytest 和 JUnit 专用 Fixture。
- 所有导出统一为 ZIP 包，内附 `manifest.json`。
- Manifest 记录项目、Schema、种子、生成器版本、数量、异常比例、文件字节数和 SHA-256。
- 新增完整交付包：全量 JSON、每表 CSV、MySQL/PostgreSQL/SQLite 和 Markdown 覆盖报告。

### 安全与质量

- 使用标准 SHA-256，包含 Web Crypto 快路径和兼容实现，并通过标准摘要向量测试。
- ZIP 由本地无压缩存储模式生成，不引入新的第三方运行时依赖。
- CSV、JUnit CSV 和 XLSX 继续统一阻断公式注入。

## 2.0.0 - Schema 建模版

### 新增

- 支持粘贴或上传 OpenAPI 3.x JSON，从 `components.schemas` 建立多表项目。
- 支持普通 JSON 对象、对象数组和包含多个对象数组的业务样例。
- 自动推断数字、布尔、日期、对象和字符串类型，以及 email、uuid、URL、IP 等生成器。
- 自动识别枚举、最小/最大值、最大长度、可空和必填字段。
- 根据常见 `userId` / `user_id` 形式连接目标表主键。
- 导入前展示来源、表数、字段数、字段摘要和截断告警，确认后才替换当前项目。

### 安全与隐私

- Schema 文件限制 1 MB、最多 50 张表、每表最多 200 个字段。
- 解析完全发生在浏览器本地，不解析远程 `$ref`、不请求外部 URL、不执行输入代码。
- 导入后继续经过生成前约束诊断，当前项目仍可通过撤销或快照恢复。

## 1.9.0 - 结果工作台版

### 新增

- 数据表头可以锁定字段，主键默认且始终锁定。
- 支持保留锁定字段后重新生成单行，外键仍从已生成父表中选择。
- 新增字段统计页：缺失、NULL、唯一值数、唯一率、范围和高频值 Top 3。
- 手工编辑根据字段类型解析数字、布尔值与 `NULL`。

### 改进

- 修改单元格、删除数据行和重新生成单行后，数据总量及质量报告立即重算。
- 单行重生成遵守公式、条件字段、异常模式、唯一性及自定义数据池规则。
- 切换模板或恢复项目时清理旧表字段锁，避免无关状态残留。

## 1.8.0 - 项目快照版

### 新增

- 项目面板支持创建最多 20 个命名快照，记录名称、时间、表数和字段数。
- 快照可一键恢复，并进入现有撤销历史，误恢复后仍能撤销。
- 选择快照后实时比较当前项目，展示项目、数据表和字段三级差异。
- 差异覆盖项目名称、模式、种子、表数量/名称和字段全部规则属性。

### 安全与隐私

- 快照只保存 Schema，不保存生成结果和自定义业务数据池。
- 快照仅写入当前浏览器 IndexedDB，读取时重新执行完整项目配置校验。
- 单快照限制 1 MB，损坏或非本工具格式的数据会被忽略。

## 1.7.0 - 离线可用版

### 新增

- 新增 PWA Manifest、应用图标和可安装的独立窗口体验。
- 构建时自动生成带版本指纹的 Service Worker 预缓存清单。
- 首次完整访问后，断网仍可打开工作台、生成数据和读取浏览器本地项目。
- 检测到新版本后显示更新提示，由用户确认后再刷新页面。
- 离线时显示明确状态，不阻塞本地功能使用。

### 安全与部署

- Service Worker 只拦截同源 GET 静态资源，不处理第三方地址和写请求。
- 旧版本缓存激活后自动清理，`sw.js` 在 Cloudflare Pages 上禁止长期缓存。
- 页面导航使用网络优先策略，在线时优先拿到最新版本，断网时回退缓存。

## 1.6.0 - 状态链实战版

### 新增

- 订单、支付、物流和游戏任务状态链支持设置实体数量和目标终态。
- 支持配置每次状态迁移的最短与最长停留分钟数。
- 可定向注入状态倒退、跳过状态或重复事件，并标记异常类型。
- 事件增加停留时长、稳定 Trace ID 和连续序号，可导出 JSON 或 CSV。

### 改进

- 状态链时间基准固定，相同种子和配置可以得到完全一致的事件流。
- 实体数量、停留时间和页面预览均设上限，大任务导出仍包含完整事件。

## 1.5.0 - Pairwise 实战版

### 新增

- Pairwise 维度可以新增、编辑和删除，支持 2–8 个维度、每维最多 20 个候选值。
- 支持按“维度=值”排除不合法组合，并在构造过程中保持约束成立。
- 支持强制包含关键组合，未指定维度由算法自动补全。
- 实时审计所有仍合法的二元参数对，显示实际覆盖率和未覆盖项。
- 一键导出 JSON、CSV 和 Postman Collection Runner 数据。

### 安全与质量

- 组合算法完全确定，相同输入稳定得到相同用例集。
- 限制维度、候选值、规则行数和规则文本长度，降低公开站点资源滥用风险。
- 强制组合与排除规则冲突、未知维度及未知候选值会给出明确错误。

## 1.4.0 - 条件规则版

### 新增

- 字段编辑器新增可视化条件规则，可组合 AND 与 OR，最多配置 10 条规则。
- 支持等于、不等于、包含、大于、小于、为空和不为空七种操作。
- 条件不满足时可以将字段设为 `NULL`，也可以从记录中移除字段。
- 电商支付记录默认演示“状态等于成功时才生成支付时间”。

### 安全与质量

- 条件规则只执行白名单比较操作，不解析或运行 JavaScript。
- 诊断器会发现条件引用自身、引用已删除字段和比较值缺失。
- 项目配置导入校验条件数量、操作符、字段名和值长度。

## 1.3.0 - 约束诊断版

### 新增

- 配置修改后即时检查循环依赖、失效引用、表名及字段名冲突。
- 检查数值范围、唯一枚举容量、公式字段引用、空值率与生成总量。
- 新增独立诊断面板，支持点击问题定位到对应数据表和字段。

### 改进

- 生成前存在阻断问题时不启动生成任务，避免无效大任务占用浏览器资源。
- 警告不会阻断正常生成，并给出明确修复建议。

## 1.2.0 - 建模效率版

### 新增

- 新增、复制、重命名、排序和删除数据表。
- 项目结构修改支持最多 30 步撤销与重做。
- 新增数据表关系页，展示生成顺序和跨表字段引用。

### 改进

- 被其他表引用的数据表禁止直接删除，避免产生失效 Schema。
- 新建表自动提供可用的自增主键，复制表自动处理名称及字段 ID。

## 1.1.0 - 安全公开版

### 新增

- 项目配置下载与恢复，配置文件使用专用格式和版本号。
- 一键恢复内置模板、清除当前浏览器本地数据。
- 数据池导入的体积、数量、名称与内容限制。
- Cloudflare Pages 静态资源长期缓存规则。

### 安全

- 导入配置通过 Zod 校验表、字段、行数、名称、引用和数值范围。
- CSV、TSV、XLSX 导出阻断电子表格公式注入。
- SQL 表名和字段名按数据库方言进行标识符转义。
- 增加 CSP、禁止嵌入、跨源隔离、权限限制和 MIME 嗅探保护。

## 1.0.0 - 首个公开版本

- 120+ 字段生成器、7 套场景模板和多表关联造数。
- 随机、真实分布、边界、异常及 Pairwise 模式。
- 质量检查、覆盖报告、状态链、多格式导出和本地数据池。
