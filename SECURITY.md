# 安全策略

## 支持范围

当前 `main` 分支和公开部署的最新版本接受安全修复。旧版本不会单独维护，请先确认问题能在最新版本复现。

## 私下报告漏洞

请进入仓库的 **Security** 页面，优先使用私密漏洞报告或 Security Advisory。不要在公开 Issue 中粘贴可利用细节、真实项目配置、用户数据、访问令牌或内部接口地址。

报告请尽量包含：受影响版本、风险说明、最小复现步骤、浏览器环境和建议修复方向。所有样例必须脱敏。

如果仓库尚未启用私密漏洞报告，请只通过维护者 GitHub 主页联系，不要先公开利用细节。

## 安全边界

- 产品是 Cloudflare Pages 静态站点，没有数据库、登录系统或匿名写入 API。
- 用户配置和生成结果默认只在当前浏览器处理；分享链接只包含 Schema 与种子，且不提供加密或访问控制。
- 导入文件和项目规模都有上限，解析器不执行用户输入代码，也不解析网络或文件系统 `$ref`。
- 公开仓库代码可被阅读和复制；安全目标是保护部署、访问者隔离和本地数据，不是隐藏客户端源码。

